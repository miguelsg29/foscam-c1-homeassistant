# Copia este archivo a .env y rellénalo SOLO en local (.env está en .gitignore).
# Lo usa tools/probe_camera.py si prefieres no escribir los datos cada vez.
FOSCAM_HOST=192.0.2.10
FOSCAM_PORT=443
FOSCAM_USER=camera_user
FOSCAM_PASSWORD=
